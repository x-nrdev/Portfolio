---
const { class: className = "", name, id, placeholder, required } = Astro.props;
---

<textarea
    class={`${className} w-full bg-secondary rounded-md text-white px-2 py-1`}
    name={name}
    id={id}
    placeholder={placeholder}
    required={required}></textarea>
