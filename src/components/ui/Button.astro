---
const { class: className = "", type = "button" } = Astro.props;
---

<button
    class=`${className} text-white rounded-[10px] px-4 py-1 hover:bg-accent hover:text-black transition-all duration-200 border-2 border-accent`
    type={type}
>
    <slot />
</button>
