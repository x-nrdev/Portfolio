---
const {
    class: className = "",
    type = "text",
    name,
    id,
    placeholder,
    pattern
} = Astro.props;
---

<input
    class={`${className} w-full rounded-md bg-secondary text-white px-4 py-1`}
    type={type}
    name={name}
    id={id}
    placeholder={placeholder}
    pattern={pattern}
/>
