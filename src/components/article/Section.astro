---
const { class: className, ariaLabel } = Astro.props;
const classList = className ? `mb-section-gap ${className}` : "mb-section-gap";
---

<section class={classList} aria-label={ariaLabel}>
    <slot />
</section>
