---
import type { ArticleSkillsType } from "@src/types";
import Skill from "@components/article/ArticleSkill.astro";
const { skills } = Astro.props as ArticleSkillsType;
---

{
    skills && (
        <ul class="flex flex-wrap gap-2">
            {skills.map((skill) => (
                <Skill name={skill} />
            ))}
        </ul>
    )
}
